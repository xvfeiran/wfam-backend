DECLARE
  e_table_exists EXCEPTION;
  PRAGMA EXCEPTION_INIT(e_table_exists, -955);
BEGIN
  EXECUTE IMMEDIATE '
    CREATE TABLE APMS_SUPPLIER (
      ID   NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
      NAME VARCHAR2(100) NOT NULL
    )
  ';
EXCEPTION
  WHEN e_table_exists THEN NULL;
END;
